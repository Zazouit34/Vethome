/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Vethome',
  assetPrefix: '/Vethome/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig; 